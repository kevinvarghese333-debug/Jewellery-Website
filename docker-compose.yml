services:
  jewellery-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jewellery-website
    ports:
      - "127.0.0.1:${HOST_PORT:-8080}:80"
    restart: unless-stopped
